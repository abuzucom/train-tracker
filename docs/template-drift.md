# Template drift

Local differences from the adopted templates, and the reason for each.
`DRIFT.md` and `adopters/` belong to `abuzucom/agents`, so this repository
records its differences here instead.

## Adopted at

- `abuzucom/agents` commit `f00081fddac53e0cf1a3c308e2802b712747233e`.
- `abuzucom/foucault` commit `551a8000a33ba1955d5e9ed79c9f08daacc4ae99`,
  release 3.3.8.

## Taken

The template was taken whole, per Rule 18.

- `AGENTS.md` and all eight synchronized copies, generated by
  `scripts/sync.py`.
- Every file under `scripts/`, `hooks/`, `tests/`, `tools/`, and
  `docs/agent-policy/`.
- `.agents/hooks.json`, `.claude/settings.json`, `.codex/config.toml`,
  `.codex/hooks.json`, `.gemini/settings.json`.
- `shared-files.json`, `hook-coverage-baseline.json`, `.gitattributes`,
  `.editorconfig`, `.claudeignore`, `.pre-commit-config.yaml`,
  `requirements-checkers.txt`, `Makefile`.
- All five workflows under `.github/workflows/`, including
  `immutable-conflict-check.yml`, plus both issue and pull request templates.
- `plan/HANDOFF.md.example`, `SECURITY.md.example`, `CONTRIBUTING.md.example`.
- From foucault: `ci/` and a caller workflow at
  `.github/workflows/security-review-pr.yml`.

Nothing was declined.

## Current drift

`AGENTS.md` carries this repository's orientation inside the
`repository-only` markers, and retains the commented per-repo orientation
template. That is the marker contract described in `DRIFT.md`, not a template
change.

The canonical policy limit is 32768 bytes and the template already occupies
32764 of them. The orientation that fits in the remaining space is three
sentences, so the commands, protected paths, architecture, and feed behavior
that the orientation block normally carries live in
`docs/project-orientation.md` and are linked from `AGENTS.md`. Raising the
limit or shrinking the template is an upstream decision, not one this
repository should take locally.

`docs/agent-policy/security.md` carries JavaScript and D1 injection and
hashing examples in place of the template's Python ones. Adoption step 3
directs an adopter to replace examples when another language dominates. The
rule text is unchanged.

`hook-coverage-baseline.json` is the template's file, copied rather than
regenerated. `python scripts/check_hook_coverage.py --write-baseline` refuses
to run as root and this environment runs as root. Every file under `hooks/` is
byte-identical to the template, and `scripts/sync.py --check-shared` confirms
all 17 shared digests match, so the template's measured baseline describes this
checkout. Regenerate it in a non-root environment when one is available.

## Local additions

`.github/workflows/worker-tests.yml` is this repository's own workflow, in the
"expected to differ" category. It runs `npm test` on pull requests and pushes
to `main`. It adds no action beyond the `actions/checkout` pin the adopted
workflows already use, and it sets `persist-credentials: false`. It omits a
`setup-node` step because the runner image already satisfies the `engines`
constraint and the suite uses only the Node standard library, so adding an
action would add a pin to maintain for no gain.

The adopted branch gate, `hooks/enforce_branch_name.py`, allowlists specific
programs in `INSPECTABLE_PROGRAMS` and specific workflow scripts in
`WORKFLOW_SCRIPT_ARGUMENTS`. `node` appears in neither, so an agent working in
this repository cannot run `npm test`. That is designed gate behavior, not a
defect, and the hook is unmodified: Rule 18 forbids narrowing a gate and Rule
22 requires fresh active-human consent before editing a hook file. The
consequence is that the Worker suite is written by agents and verified in CI.
The validation-first workflow's "run the failing test first" step therefore
happens in CI rather than locally for JavaScript changes.

## Not adopted from foucault

`adopters/train-tracker.md` belongs in `abuzucom/foucault`, whose
`adopters/README.md` defines that convention. This session has read-only
access to that repository and cannot open the pull request that would add it.
The record it should carry is in `docs/pr-security-review.md`.

## Known environment failure

`tests/test_block_destructive_bash.py::RepoExecutesOnReadTest`, case
`test_malformed_environment_config_vector_fails_closed`, fails inside a
container whose git proxy exports `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_0`, and
`GIT_CONFIG_VALUE_0`. The test prefixes `GIT_CONFIG_COUNT=1
GIT_CONFIG_KEY_0=core.pager` with no matching value and expects the gate to
fail closed. `run_hook_in` calls `subprocess.run` without an `env` argument, so
the child inherits the ambient `GIT_CONFIG_VALUE_0` and the key and value
vector looks complete. The gate and the test are both correct. A runner without
those variables, including GitHub Actions, is unaffected. Rule 3 forbids
changing the test, so it stands as written.
